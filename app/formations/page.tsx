import type { Metadata } from "next";
import { PageIntro } from "../_components/PageIntro";
import { formations } from "../_data/content";
export const metadata: Metadata = { title: "Formations", description: "Formations et qualifications techniques de Daniel Cruz." };
export default function FormationsPage() { return <><PageIntro eyebrow="Formation continue" title="Des bases techniques solides, une progression constante" lead="Des formations professionnelles complétées par un apprentissage pratique des systèmes et des réseaux." /><section className="section section-shell"><div className="formation-list">{formations.map((item) => <article key={item.title}><div><span className="status">{item.status}</span><span>{item.period}</span></div><div><p className="eyebrow">{item.category}</p><h2>{item.title}</h2><strong>{item.institution}</strong><p>{item.description}</p></div></article>)}</div><div className="learning-now"><span className="pulse" aria-hidden="true" /><div><p className="eyebrow">En cours</p><h2>Transition vers l’administration IT</h2><p>Laboratoire personnel consacré à Windows Server, Active Directory, DNS, réseaux, PowerShell, Proxmox VE et postes clients.</p></div></div></section></>; }
