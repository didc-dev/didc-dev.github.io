import type { Metadata } from "next";
import { PageIntro } from "../_components/PageIntro";
import { formations } from "../_data/content";
export const metadata: Metadata = { title: "Formations", description: "Diplômes terminés et apprentissages actuels de Daniel Cruz." };
export default function FormationsPage() { return <><PageIntro eyebrow="Qualifications & progression" title="Des bases techniques solides, une progression documentée" lead="Les diplômes terminés sont distingués des compétences actuellement approfondies dans le laboratoire personnel." /><section className="section section-shell"><div className="formation-list">{formations.map((item) => <article key={item.title}><div><span className="status">{item.status}</span><span>{item.period}</span></div><div><p className="eyebrow">{item.category}</p><h2>{item.title}</h2><strong>{item.institution}</strong><p>{item.description}</p><div className="tags">{item.skills.map((skill) => <span key={skill}>{skill}</span>)}</div></div></article>)}</div><div className="learning-now"><span className="pulse" aria-hidden="true" /><div><p className="eyebrow">En cours — pas une certification obtenue</p><h2>Administration IT, systèmes et réseaux</h2><p>Windows Server, Active Directory, DNS, PowerShell, Proxmox VE et postes clients sont travaillés progressivement avec des preuves et des validations.</p></div></div></section></>; }
