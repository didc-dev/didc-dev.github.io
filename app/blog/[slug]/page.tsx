import Link from "next/link";
import { notFound } from "next/navigation";
import { articles } from "../../_data/content";
export function generateStaticParams() { return articles.filter((article) => article.status === "published").map((article) => ({ slug: article.slug })); }
export default async function ArticlePage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const article = articles.find((item) => item.slug === slug && item.status === "published"); if (!article) notFound(); const dns = slug.startsWith("dns"); return <article className="article-page section-shell"><Link className="back-link" href="/blog/">← Blog technique</Link><p className="eyebrow">{article.category} · {article.reading}</p><h1>{article.title}</h1><p className="article-lead">{article.excerpt}</p>{dns ? <div className="article-body"><h2>À quoi sert le DNS?</h2><p>Les équipements communiquent avec des adresses IP, mais les personnes retiennent plus facilement des noms. Le Domain Name System traduit ces noms en informations utilisables par le réseau.</p><h2>Pourquoi est-il essentiel dans Active Directory?</h2><p>Un domaine Windows publie dans DNS les services qui permettent aux postes de localiser un contrôleur de domaine. Un DNS incorrect peut donc empêcher une ouverture de session, une jonction au domaine ou l’application de stratégies.</p><h2>Le réflexe de diagnostic</h2><p>Vérifier d’abord l’adresse IP, le serveur DNS configuré, puis la résolution du nom. Cette progression évite de corriger le mauvais composant.</p><p className="source-note">Références: RFC 1034 et documentation Microsoft Learn sur DNS. Vérifiées le 28 juillet 2026.</p></div> : <div className="article-body"><h2>Plus qu’une fenêtre noire</h2><p>PowerShell est un environnement d’administration orienté objets. Ses commandes peuvent interroger un système, filtrer les résultats et appliquer une modification de manière reproductible.</p><h2>Observer avant de modifier</h2><p>Dans un laboratoire, PowerShell sert d’abord à confirmer un état: services actifs, configuration réseau, machines virtuelles ou objets Active Directory. Une commande de validation constitue aussi une preuve technique.</p><h2>Apprendre progressivement</h2><p>Comprendre le pipeline, les objets et <code>Get-Help</code> apporte plus de valeur que mémoriser de longues commandes sans contexte.</p><p className="source-note">Référence: documentation officielle Microsoft Learn PowerShell. Vérifiée le 28 juillet 2026.</p></div>}</article>; }
